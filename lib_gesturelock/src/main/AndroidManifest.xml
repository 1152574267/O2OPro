<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.mrk.gesturelock">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name=".ui.activity.CreateGestureActivity" />
        <activity android:name="com.mrk.yinxiangzhe.ui.activity.GestureLoginActivity" />
    </application>

</manifest>
